# Пароль для входа в /admin (только на сервере, не в Vite)
ADMIN_PASSWORD=ZaLupaPonGG

# Дубль; основной .env — в корне репозитория.
API_PORT=4000

# Опционально для продакшена: полный URL бэкенда, если фронт отдаётся с другого хоста
# VITE_API_URL=https://api.example.com
